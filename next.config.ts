/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['freepik.com', 'pexels.com',"img.freepik.com"],
  },
  // Other Next.js config options can go here
}

export default nextConfig